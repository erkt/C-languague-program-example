#include<stdio.h>
main(){
int	char 	float	double	    union 	struct		typedef
signed  	unsigned    	long    short        for    if    else     while
return    volatile      const     break     continue     switch    case    default
void    goto    do    enum    auto    static       extern    register   sizeof 
}
